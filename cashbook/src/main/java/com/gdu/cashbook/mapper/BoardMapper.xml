<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gdu.cashbook.mapper.BoardMapper">
	<!-- 게시글 작성 -->
	<insert id="" parameterType="com.gdu.cashbook.vo.Board"></insert>
	<!-- 게시글리스트 -->
	<select id="selectBoardList" parameterType="com.gdu.cashbook.vo.Board" resultType="com.gdu.cashbook.vo.Board">
		SELECT
			board_no as boardNo,
			member_id as memberId,
			board_title as boardTitle,
			board_date as boardDate
		FROM board	
	</select>
	<!-- 게시글 상세보기 -->
	<select id="selectBoardOne" parameterType="com.gdu.cashbook.vo.Board" resultType="com.gdu.cashbook.vo.Board">
		SELECT
			board_no as boardNo,
			member_id as memberId,
			board_title as boardTitle,
			board_contents as boardContents,
			board_date as boardDate
		FROM board
		WHERE board_no = #{boardNo}
	</select>
</mapper>